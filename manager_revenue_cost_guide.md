# 📊 دليل الأرباح والتكاليف لمدير تطبيق (صيدلي PRO)

بصفتك **مدير التطبيق (Application Manager/Owner)**، يعتمد نموذج عمل التطبيق على توازن دقيق بين الإيرادات المحققة من الصيدليات والتكاليف التشغيلية التي تستهلكها كل صيدلية على السيرفر (Supabase). يوضح هذا الدليل بالتفصيل مصادر أرباحك، وتكاليفك، وكيفية احتسابها بدقة.

---

## 💵 أولاً: مصادر الأرباح (Revenues)

تتحقق أرباحك كمدير للتطبيق من خلال ثلاثة مسارات رئيسية:

### 1. اشتراكات الصيدليات (Subscriptions)
وهي المصدر الأساسي والداعم للتطبيق، وتتم عن طريق بيع **أكواد تفعيل الاشتراكات** (`subscription_codes`):
* **باقة بريميوم (Premium Plan):** تتيح للمستخدم تشغيل كافة ميزات التطبيق (المقترح: 199 ج.م/شهرياً).
* **باقات المساعدين (Assistants Plans):**
  * باقة 3 مساعدين (99 ج.م/شهرياً).
  * باقة المساعد الإضافي (تسعير تصاعدي: 49 ← 99 ← 149 ج.م).
* **الباقة المدمجة (Premium + Assistants):** تشمل كافة الميزات مع 3 مساعدين (300 ج.م/شهرياً).

### 2. الإعلانات المدفوعة (In-App Advertisements)
تأجير المساحات الإعلانية داخل التطبيق (`ads`) لشركات الأدوية، الموزعين، والمستودعات الطبية:
* **إعلانات مستهدفة جغرافياً:** عرض إعلانات محددة لصيدليات دولة معينة (`target_country`).
* **إعلانات مستهدفة سلوكياً:** عرض الإعلانات في شاشات معينة (الرئيسية، النواقص، أو التقارير) لضمان أعلى معدل وصول وتفاعل.
* **التواصل المباشر عبر الواتساب:** إتاحة زر تواصل مباشر داخل الإعلان مما يسمح لك بمحاسبة المعلن بنظام (الدفع مقابل النقرة CPC) أو (الدفع مقابل طلب العرض).

### 3. عمولات طلبات المندوبين (Rep Commissions - مستقبلاً)
عندما تقوم الصيدلية بإرسال نواقصها أو طلبياتها لمندوبي الشركات عبر التطبيق، يمكن احتساب عمولة بسيطة من المندوب أو الموزع عن كل طلبية ناجحة يتم تمريرها من خلال المنصة.

---

## 📉 ثانياً: التكاليف والمسؤوليات (Costs & Expenses)

كل صيدلية تستخدم التطبيق تستهلك موارد على السيرفر السحابي (Supabase)، مما يترتب عليه تكاليف تدفعها أنت كمدير للتطبيق:

### 1. استهلاك قاعدة البيانات السحابية (Database Database & API Bandwidth)
* **المزامنة والنسخ الاحتياطي:** تقوم كل صيدلية برفع ومزامنة بياناتها بشكل دوري (الفواتير، النواقص، معاملات الديون، المساعدين، وتواريخ الصلاحية).
* **التأثير على التكلفة:** زيادة حجم البيانات المخزنة (Storage Size) وزيادة عدد طلبات السيرفر (API Requests / Read & Write Operations) يرفعان من تكلفة باقة Supabase الخاصة بك.

### 2. مساحة تخزين الوسائط والصور (Storage for Media & Public URL)
* **صور المنتجات والعملاء:** رفع صور العملاء لتأكيد الهوية وإيصالات سداد الديون (`customer-photos` و `debt-receipts`).
* **صور الإعلانات:** الصور الدعائية المرفوعة من لوحة المطور (`ads-images`).
* **التأثير على التكلفة:** استهلاك مساحة تخزين الملفات (File Storage) في Supabase، واستهلاك الباندويث المخصص لتحميل الصور (CDN Bandwidth).

### 3. رسوم تشغيل الحسابات الرسمية للـ Stores
* **Google Play Console:** رسوم تدفع مرة واحدة قيمتها $25 لفتح حساب المطور.
* **Apple Developer Program:** رسوم سنوية قيمتها $99 لتشغيل التطبيق على هواتف iOS.

---

## ⚖️ ثالثاً: معادلة صافي أرباح المدير من كل صيدلية

لكي تعرف هل صيدلية معينة مربحة لك كمدير للتطبيق أم أنها تستهلك موارد سيرفر أكثر مما تدفع:

$$\text{صافي ربح المدير} = (\text{قيمة كود الاشتراك المستهلك} + \text{أرباح الإعلانات الموجهة لها}) - (\text{تكلفة تخزين بياناتها} + \text{تكلفة الباندويث المستهلك للمزامنة})$$

### جدول تقديري للأرباح والتكاليف (لكل صيدلية/شهرياً)

| البند | النوع | القيمة التقديرية (ج.م) | ملاحظات |
| :--- | :--- | :--- | :--- |
| **اشتراك باقة بريميوم** | إيراد (Revenue) | +199 | ثابت شهرياً لكل صيدلية نشطة |
| **مشاهدة/تفاعل مع الإعلانات** | إيراد (Revenue) | +20 إلى +50 | متغير حسب تفاعل الصيدلية مع العروض المتاحة |
| **استضافة السيرفر وقاعدة البيانات** | تكلفة (Cost) | -5 إلى -15 | تكلفة تخزين الجداول (Supabase DB Storage) |
| **استهلاك الباندويث لرفع الصور** | تكلفة (Cost) | -3 إلى -10 | متغير حسب عدد صور إيصالات الديون المرفوعة |
| **صافي الربح التقديري للمدير** | **صافي ربح** | **+200 ج.م تقريباً** | معدل ربح ممتاز جداً يتجاوز 90% لكل صيدلية |

---

## 🛠️ رابعاً: كيف تتابع وتستعلم عن أرقامك من Supabase؟

بصفتك مدير التطبيق، يمكنك تشغيل هذه الاستعلامات (SQL Queries) مباشرة في **Supabase SQL Editor** لمتابعة أرباحك وتكاليفك من الصيدليات:

### 1. استعلام لمعرفة عدد الصيدليات النشطة وتاريخ انتهاء اشتراكها
يساعدك في معرفة الصيدليات الملتزمة بالدفع:
```sql
SELECT 
  id AS pharmacy_id,
  name AS pharmacy_name,
  pharmacy_code,
  created_at,
  -- جلب تاريخ انتهاء الاشتراك من إعدادات الصيدلية
  (SELECT value FROM pharmacy_assistants WHERE role = 'owner' LIMIT 1) as subscription_expiry
FROM pharmacies
ORDER BY created_at DESC;
```

### 2. استعلام لمعرفة الأكواد الأكثر مبيعاً واستخداماً
لمعرفة أي الباقات تحقق لك أعلى مبيعات:
```sql
SELECT 
  code,
  plan,
  duration_days,
  discount_percent,
  used_count,
  max_uses,
  (used_count * 199) AS estimated_revenue -- افتراض باقة بريميوم 199 ج.م
FROM subscription_codes
ORDER BY used_count DESC;
```

### 3. استعلام لقياس استهلاك كل صيدلية لقاعدة البيانات (مؤشر التكلفة)
الصيدلية التي تملك عدداً ضخماً من الفواتير والنواقص تستهلك مساحة أكبر على السيرفر وبالتالي تمثل تكلفة تشغيلية أعلى:
```sql
SELECT 
  p.name AS pharmacy_name,
  COUNT(DISTINCT i.id) AS total_invoices_synced,
  COUNT(DISTINCT s.id) AS total_shortages_synced,
  COUNT(DISTINCT e.id) AS total_expiries_synced
FROM pharmacies p
LEFT JOIN pharmacy_invoices i ON i.pharmacy_id = p.id
LEFT JOIN pharmacy_shortages s ON s.pharmacy_id = p.id
LEFT JOIN pharmacy_medication_expiries e ON e.pharmacy_id = p.id
GROUP BY p.name
ORDER BY total_invoices_synced DESC;
```

---

> [!TIP]
> **نصيحة لتحسين الأرباح:**
> قم دائماً بتشجيع الصيدليات على استخدام باقة البريميوم المدمجة مع المساعدين، وقم ببيع مساحات إعلانية لشركات الأدوية المحلية التي تبحث عن تسويق نواقصها مباشرة إلى الصيدليات، حيث يمثل التطبيق قناة تسويقية ذات قيمة عالية جداً ومستهدفة بدقة.
